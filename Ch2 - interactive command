# Ch2 Interactive Command - raw_input 
# Example 2.3 Write a program to prompt the user for hours and rate per hour using raw_input to compute gross pay. 

hrs = raw_input("enter hours:").   # users can input any number, key word: raw-input 
rat = raw_input("rate:")           
pay = float(hrs)* float (rat)      # review data type, snytax_ datatype()
print pay 
